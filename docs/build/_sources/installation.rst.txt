Installation
============

Install via pypi
----------------

PyGRO is distributed as a Python package that can be installed through the PyPi package manager via:

.. code-block:: console

   $ pip install pygro